using UnityEngine;

public class MusicLoop : MonoBehaviour
{
    private static MusicLoop instance;

    private void Awake()
    {
        if(instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
